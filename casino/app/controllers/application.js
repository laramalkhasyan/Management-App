import Controller from '@ember/controller';

export default Controller.extend({
  // offset: 1,
  // gameController: Ember.inject.controller('games'),
  // actions: {
  //   detectScroll() {
  //     window.scroll(function() {
  //       if (window.scrollTop() + window.height() == document.height()) {
  //         console.log('hi');
  //         this.offset++
  //         var c = this.get('gameController');
  //         c.set('offset',this.offset);
  //       }
  //     });
  //   }
  // }
});
